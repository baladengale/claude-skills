# Change History

## 2026-02-21 — Initial Setup & Openclaw Removal

### Added
- `README.md` — Full documentation covering both skills: CLI reference, build targets, env vars, data sources, scoring algorithm, Claude Code integration, repo structure, and quick start guide.

### Changed — Both Skills (`market-overview`, `tech-intel`)
- **`loadEnvFile()`** — Changed from `~/.openclaw/.env` to `./.env` (current working directory). Place `.env` next to the binary before running.
- **`saveFallback()`** — Now saves HTML output to current directory instead of `~/.openclaw/workspace/`.
- **`Makefile` install target** — Changed destination from `~/.openclaw/workspace/skills/...` to `~/.claude-skills/skills/...`.
- **`SKILL.md`** — Removed `openclaw:` metadata nesting; updated env var docs and build paths.
- **`go.mod`** — Module renamed: `openclaw-*` → `claude-skills-*`.
- **`template.html`** — Footer branding updated from "OpenClaw" to "Claude Skills".
- **`main.go` (market-overview)** — Removed `path/filepath` import (no longer needed); updated inline HTML template footer.

### Result
Zero `openclaw` references remain anywhere in the repo. Skills work on any machine — just drop a `.env` file in the directory you run the binary from.
