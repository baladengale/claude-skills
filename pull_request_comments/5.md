Updated instructions: Please **skip `market-overview` and `tech-intel`** — do NOT convert those two skills and do NOT delete their Go files (`main.go`, `go.mod`, `Makefile`, `template.html`). Leave them exactly as they are. Only convert the remaining 22 skills (all bash-based debug skills + `k8s-doctor` + `sre-intel`).